# 회고 — 자가 촬영 상품 사진 배경/조명 정제 PoC

## 1. 적은 세 지점 중에서 고른 것과 뺀 것의 차이는 무엇이었습니까?

[`MODEL_SELECTION.md`](docs/MODEL_SELECTION.md)에 두 단계 각각 3개 후보를 적어뒀다.

**① 상품 영역 분할 — YOLOv8-seg / SAM / rembg**
- SAM은 뺐다: 정확도는 제일 높다고 알려져 있지만, 포인트/박스 프롬프트 로직을 따로 설계해야 하고 무거워서
  "20장을 빠르게 일괄 처리"라는 이번 목적과 안 맞았다.
- 처음엔 YOLOv8-seg를 골랐다: 학습된 클래스 기반이라 바로 쓸 수 있고 구현이 제일 빨랐다.
- 이후 rembg로 바꿨다: 처음엔 라이선스를 전혀 의식하지 않고 YOLOv8-seg 그대로 커밋·푸시할 계획이었는데,
  커밋 전에 다른 수강생들의 과제 README를 둘러보다가 라이선스 문제를 뒤늦게 의식하게 됐다. 마침
  YOLOv8-seg가 COCO 80종 밖 상품(효도박스, 스피커 등)을 아예 탐지 못 하는 실패도 잦았던 걸 이미 알고
  있던 터라, "상업적으로 쓸 수 있는 라이선스 중엔 YOLO보다 나은 게 없을까"라는 생각으로 클래스 인식
  없이 전경/배경만 분리하는 다른 방식을 비교해봤다. 결과적으로 rembg가 20장 전체에서 탐지 실패 없이
  동작해 최종 채택했다.
- **고른 것과 뺀 것의 차이**: SAM은 "정확하지만 무겁고 프롬프트가 필요함"이라 애초에 후보에서 제외했고,
  YOLOv8-seg는 "빠르지만 학습된 클래스 밖은 아예 인식 못 함", rembg는 "클래스 인식은 없지만 전경/배경
  분리 자체는 안정적으로 됨" — 결국 **클래스 지식이 필요 없는 방식이 이번처럼 상품 종류가 다양하고
  학습 데이터에 없는 케이스에는 더 안정적이었다.**

**② 배경 재생성 — Diffusion inpainting / LaMa / OCR+inpainting**
- OCR+inpainting은 뺐다: 애초에 "워터마크·텍스트 제거" 문제로 기획했던 흔적이라, "배경 전체 재생성"으로
  문제를 재정의한 뒤에는 결이 맞지 않았다.
- LaMa도 뺐다: 국소 영역을 지우는 데는 diffusion보다 안정적이지만, "빈 배경을 새로운 스튜디오 장면으로
  채운다"는 이번 목표에는 덜 맞는다고 판단했다.
- Diffusion inpainting을 골랐다: 넓은 영역도 자연스러운 텍스처로 채울 수 있다는 게 이유였는데, 실제로는
  이 장점이 "오생성"(장식품·소품을 제멋대로 만들어냄)이라는 단점으로 그대로 돌아왔다 — 고른 이유와
  실패 원인이 같은 지점에서 나왔다는 게 이번 PoC에서 가장 중요하게 확인한 사실이다.

## 2. 성공 기준의 숫자를 어디서 가져왔습니까? 현행 수준을 실제로 재 보았습니까?

[`PROBLEM_DEFINITION.md`](docs/PROBLEM_DEFINITION.md)의 "80% 이상"이라는 숫자는 **측정해서 나온 값이 아니라
임의로 잡은 목표치**다. "이 정도면 검수 없이 쓸 만하겠다"는 감으로 정한 것이지, 현재 수작업 방식의
실제 성공률이나 실패율을 먼저 재고 거기서 역산한 게 아니다.

반대로 **처리 시간 쪽은 실측했다.** [`RESULTS.md`](docs/RESULTS.md)의 "수작업 대비 처리 시간 단축 폭"
항목을 위해 사진 한 장(12_toothpick_bathroom)을 실제로 클립스튜디오로 배경 제거해보고 10분이 걸리는 걸
직접 쟀다. 즉 **양적 기준(처리 시간)은 베이스라인을 측정했지만, 품질 기준(80%)은 측정 없이 잡은 목표치**였다는
비대칭이 있다. 결과적으로 실제 성공률은 50%(rembg 기준, 1차 YOLO 기준으로는 35%)로 나왔는데, 이걸
"80%에 크게 못 미친다"고 판단할 근거 자체가 애초에 감으로 잡은 숫자와의 비교라서, 이 기준이 정말
적절했는지는 다시 따져볼 여지가 있다.

## 3. 실행 위치나 라이선스 때문에 제외한 후보가 있었습니까? 그 후보가 점수로는 몇 위였습니까?

**실행 위치** 때문에 제외한 후보는 없다. 로컬(RTX 3060)에서 전부 돌렸고, 클라우드/로컬 비교 자체를
해본 적이 없다.

**라이선스** 문제는 있었고, 계기 자체가 라이선스와 완전히 무관하지도 않다. YOLOv8-seg는 애초에 점수
(실용성) 1위로 채택했던 모델이다 — 학습된 모듈 계열이라 구현이 제일 빠르고 근거도 명확해서 1차 PoC에서
실제로 채택했고, 커밋·푸시 시점까지도 라이선스는 전혀 확인하지 않았다. 그런데 커밋 직전 다른 수강생들의
과제 README를 둘러보다가 Ultralytics YOLOv8이 AGPL-3.0 라이선스라는 걸 뒤늦게 의식하게 됐다. 그 시점에
YOLOv8-seg의 실패율(COCO 80종 밖 상품 미탐지)도 이미 알고 있었기 때문에, "어차피 탐지 실패도 잦은데
상업적으로 쓸 수 있는 라이선스 중엔 더 나은 게 없을까"라는 생각으로 rembg를 비교해봤다(자세한 경위는
[`MODEL_SELECTION.md`](docs/MODEL_SELECTION.md) 참고). 그래서 이건 "실사용 계획이 있어서 라이선스 때문에
바꿨다"도 아니고 "라이선스와는 전혀 무관하게 궁금해서 봤는데 겸사겸사 라이선스 문제까지 확인됐다"도
아니다 — 정확히는 **라이선스를 몰랐다가 다른 사람 작업물을 보고서야 의식한 계기와, 이미 갖고 있던 정확도
불만이 동시에 작용해 교체로 이어진 경우**에 가깝다. "라이선스 때문에 제외한 후보"라는 질문에 맞춰 답하자면,
YOLOv8-seg는 라이선스만으로 제외된 게 아니라 라이선스 인지 + 실패율 두 가지가 겹쳐서 교체됐다.

## 4. 기준을 넘지 못한 항목이 있다면, 다음에 무엇을 바꿔 다시 확인하겠습니까?

[`RESULTS.md`](docs/RESULTS.md)의 성공 기준 3개 중 ①(잔여물 없는 비율 80% 이상)과 ③(자연스러워 바로 쓸
수 있는 비율)을 넘지 못했다 — 둘 다 50%로, rembg 교체 후에도 여전히 미달이다. ②(처리 시간 단축)는
18배 단축으로 기준 자체를 명시적 수치로 잡진 않았지만 목적은 달성했다.

미달의 원인은 전부 diffusion inpainting의 "오생성"이었고(마스크 품질과는 무관 — rembg로 바꿔도 그대로
남음), 다음에 바꿔볼 것은 `RESULTS.md`에 이미 적어둔 대로:

1. 네거티브 프롬프트에 `object, prop, decoration, reflection` 등을 추가해 배경에 새 사물이 생기는 것
   자체를 억제
2. SDXL 기반 inpainting이나 LaMa(비-생성형)와 비교해 오생성 빈도 차이 확인
3. 배경이 어둡거나 색이 진할수록 오생성이 잦다는 패턴이 나왔으니, 촬영 시 배경 밝기를 일정 수준
   이상으로 확보하는 촬영 가이드 마련

추가로 이번 회고에서 새로 드러난 것: 80% 목표치 자체가 측정 없이 잡은 숫자였으니, 다음 반복 전에
"애초에 몇 %가 현실적인 목표인지"를 rembg 결과(50%) 같은 실측 데이터를 기준으로 다시 잡는 것도
필요하다.
